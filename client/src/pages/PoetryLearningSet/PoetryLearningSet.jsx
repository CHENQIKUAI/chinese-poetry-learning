import React, { Component } from "react";
import { Form, Input, Button, Alert, message } from "antd"

export default class PoetryLearningSet extends Component{
    render(){
        return (
            <div>PoetryLearningSet</div>
        )
    }
} 